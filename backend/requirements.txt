fastapi
uvicorn[standard]
weave
wandb
openai
anthropic
claude-agent-sdk
supabase
pydantic-settings
sse-starlette
python-dotenv
python-multipart
httpx
pyjwt[crypto]
elevenlabs

# project-brief ingestion (deck + demo video → context)
groq                 # Whisper transcription (audio only; never the council agents)
pypdfium2            # PDF text + page→image rendering (permissive; no system deps)
pillow               # image encode/resize for slide pages + sampled frames
imageio-ffmpeg       # bundled static ffmpeg (audio extraction + keyframe sampling; no apt)

# dev
pytest
pytest-asyncio
